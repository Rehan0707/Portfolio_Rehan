PUBLIC ASSETS
=============

rehan.png
  Drop a cut-out (transparent background) PNG of Rehan here, ideally a
  portrait around 700x900px. The hero will use it automatically. Until then,
  an elegant "RS" gradient fallback renders in its place.

Rehan_Resume.pdf
  The downloadable resume linked from the hero + navbar.

Optional:
  og.png  — 1200x630 social share image (referenced in metadata if added).
  favicon.ico / icon.png — site/app icons.
